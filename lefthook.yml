pre-commit:
  jobs:
    - name: cargo clippy
      run: cargo clippy --workspace --all-targets --all-features -- -W clippy::pedantic -D warnings
    - name: cargo fmt
      run: cargo fmt
      stage_fixed: true
